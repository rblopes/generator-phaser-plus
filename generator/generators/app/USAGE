Description:
  Creates a Phaser web game project, featuring a live development environment
  ready for ES2015+ and cross-device testing.

Available Sub-generators:
  object: Creates the skeleton of a extended Phaser object class (or prefab).
  state: Creates the skeleton of a Phaser game state.

  To run a sub-generator, use:

  > yo phaser-plus:<sub-generator>
